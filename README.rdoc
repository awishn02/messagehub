== README : Message Hub

This is a very simple rails app that displays messages in order of when
they were created.

Under the hood:

* Rails 4
* Ruby 2.0.0
* Urbanairship gem -- this is used to work with iOS, sends push notifications
